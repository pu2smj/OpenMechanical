# SealWorkbench

**Status**: Placeholder — not yet implemented.

This directory is reserved for a future data-driven, parametric seal library
following the same engineering-first principles as `BearingWorkbench/` and
`ORingWorkbench/`.

## Current State

The seal command in `OpenMechanicalWorkbench.py` (`make_seal()`) currently
uses placeholder geometry (a simple ring). This will be replaced when the
`SealWorkbench` library is implemented.

## Planned Direction

- Data source: relevant seal standards (e.g., ISO, DIN, or manufacturer catalogs)
- Follow the same modular pattern as `BearingWorkbench/`
- Generate geometry from parameters, not the other way around
