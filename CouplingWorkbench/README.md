# CouplingWorkbench

**Status**: Placeholder — not yet implemented.

This directory is reserved for a future data-driven, parametric coupling library
following the same engineering-first principles as `BearingWorkbench/` and
`ORingWorkbench/`.

## Current State

The coupling command in `OpenMechanicalWorkbench.py` (`make_coupling()`) currently
uses placeholder geometry (two fused cylinders). This will be replaced when the
`CouplingWorkbench` library is implemented.

## Planned Direction

- Data source: relevant coupling standards (e.g., ISO, DIN, or manufacturer catalogs)
- Follow the same modular pattern as `BearingWorkbench/`
- Generate geometry from parameters, not the other way around
